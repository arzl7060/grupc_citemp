
    
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f8f5; /* Latar belakang hijau lembut */
            margin: 0;
            padding: 0;
        }

        h3 {
            font-size: 2rem;
            font-weight: bold;
            color: #2e7d32; /* Warna hijau daun */
            text-align: center;
            text-transform: uppercase;
            letter-spacing: 2px;
            padding: 10px;
            margin: 20px auto;
            background: linear-gradient(120deg, #a8e063, #56ab2f); /* Gradasi hijau */
            color: white;
            border: 2px solid #2e7d32;
            box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.2);
            border-radius: 10px;
            width: fit-content;
            animation: float 3s ease-in-out infinite;
        }

        /* Tambahkan animasi untuk efek "melayang" */
        @keyframes float {
            0%, 100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-10px);
            }
        }
    </style>


    <h3>Ini Dashboard</h3>
